---
name: terraviz-data-video
description: Author, validate, publish, and debug TerraViz/Zyra data-encoded datasets and video workflows from gridded sources such as GRIB2 and NetCDF. Use when creating or adding a dataset, adapting a model-cycle workflow, selecting a NOAA data source or variable, setting value ranges and palettes, writing metadata, validating publication, or troubleshooting grayscale output, missing hover values, legends, and pipeline capability gaps.
---

# TerraViz data-encoded datasets

Use the repository's canonical shared workflow in
[`../../../.claude/skills/terraviz-data-video/SKILL.md`](../../../.claude/skills/terraviz-data-video/SKILL.md).

1. Read that file completely before creating, modifying, publishing, or debugging a dataset.
2. Resolve every relative link in it from `.claude/skills/terraviz-data-video/`.
3. Load only the linked references needed for the request, and reuse the bundled scripts and templates instead of recreating them.
4. Follow its validation and verification steps before reporting completion.

Treat the `.claude` skill as the source of truth. Update the canonical skill when the workflow changes; keep this Codex wrapper limited to discovery and routing.
